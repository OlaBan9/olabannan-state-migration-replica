server {
  # Binds all interfaces (0.0.0.0) — reachable from outside the container.
  listen 8901;
  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
